# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

project_name: doublezero-solana-cli

monorepo:
  tag_prefix: doublezero-solana-cli/

builds:
  - id: doublezero-solana-cli
    binary: doublezero-solana
    builder: rust
    tool: cargo
    command: build
    flags:
      - --package=doublezero-solana-cli
      - --release
    targets:
      - x86_64-unknown-linux-gnu
    env:
      - RUSTFLAGS=-C linker=x86_64-linux-gnu-gcc
      - BUILD_VERSION={{ .Version }}
      - BUILD_COMMIT={{ .ShortCommit }}
      - DATE={{ .Date }}

archives:
  - id: doublezero_solana_archive
    formats: ["tar.gz"]
    ids: [doublezero-solana-cli]
    # this name template makes the OS and Arch compatible with the results of `uname`
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

nfpms:
  - id: doublezero-solana-cli
    package_name: doublezero-solana-cli
    ids: [doublezero-solana-cli]
    vendor: doublezero
    homepage: doublezero.xyz
    maintainer: tech <tech@malbeclabs.com>
    description: |-
      DoubleZero Solana CLI
    license: Apache 2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    release: "1"
    section: default
    # NOTE: doublezero-solana-cli does not require to be a systemd service

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: doublezerofoundation
    name: doublezero-offchain
  draft: false
  replace_existing_artifacts: true

announce:
  slack:
    enabled: true
    message_template: "DoubleZero Solana CLI {{ .Tag }} has been released! Check it out at {{ .ReleaseURL }}"
    channel: "#bots"

git:
  ignore_tags:
    - doublezero-solana-cli/daily

nightly:
  publish_release: true
  keep_single_release: true
  version_template: "{{ incpatch .Version }}~git{{ .Env.BUILD_DATE }}.{{ .ShortCommit }}"
  tag_name: "doublezero-solana-cli/daily"

cloudsmiths:
  - organization: malbeclabs
    repository: doublezero
    distributions:
      deb: "any-distro/any-version"
      rpm: "any-distro/any-version"
